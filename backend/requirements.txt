fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.10.0
python-multipart>=0.0.20
httpx>=0.28.0
# Optional, zero-cost local document engines. The API returns NEEDS_REVIEW
# instead of fabricating document text when they are unavailable.
PyMuPDF>=1.24.0
Pillow>=10.0.0
pytesseract>=0.3.10
# Zero-cost local speech recognition. The Whisper model is downloaded on first
# use into STT_MODEL_DIR (or pointed at a pre-downloaded local model).
faster-whisper>=1.1.0
# Test runner for the current FastAPI/SQLite architecture.
pytest>=8.0,<9.0
piper-tts>=1.8,<2
python-dotenv>=1.0,<2
playwright>=1.62,<2
rapidocr-onnxruntime>=1.4,<2
